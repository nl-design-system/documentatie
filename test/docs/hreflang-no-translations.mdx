---
title: Hreflang no translations test
description: Tests that only self and x-default hreflang render when no translations frontmatter is set
unlisted: true
lang: en
---

{/* @license CC0-1.0 */}

Tests that only the self-referencing `hreflang` and `x-default` link tags render when no `translations` frontmatter is set.
