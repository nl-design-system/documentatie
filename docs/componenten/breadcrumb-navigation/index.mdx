---
title: Breadcrumb Navigation
hide_title: true
hide_table_of_contents: false
sidebar_label: Breadcrumb Navigation
pagination_label: Breadcrumb Navigation
description: Toont de locatie van de pagina binnen de hiërarchie van een website en biedt de mogelijkheid om tussen niveaus te navigeren.
slug: /breadcrumb-navigation
keywords:
  - back
  - back link
  - breadcrumb navigation
  - breadcrumb trail
  - breadcrumbs
  - broodkruimelpad
  - broodkruimels
  - hiërarchie
  - hierarchy
  - home
  - homepage
  - home
  - kruimelpad
  - links
  - nav
  - navigatie
  - navigeren
  - navigation
  - trail
  - up
---

{/* @license CC0-1.0 */}

import componentProgress from "@nl-design-system/component-progress/dist/index.json";
import AcIntro from "@nl-design-system-unstable/documentation/componenten/ac/\_ac_intro.md";
import {
  DefinitionOfDone,
  HelpImproveComponent,
  Implementations,
  Introduction,
  Related,
} from "@site/src/components/ComponentPage";
import { general, visualDesign, zoom, keyboard, screenreader } from "./_criterialist.tsx";

{/* Add name and description for the component */}
export const title = "Breadcrumb Navigation";
export const description = "Toont de locatie van de pagina binnen de hiërarchie van een website en biedt de mogelijkheid om tussen niveaus te navigeren.";
export const issueNumber = 54;

export const component = componentProgress.find((item) => item.number === issueNumber);

<Introduction component={component} headingLevel={1} description={description} />

## Definition of Done

<DefinitionOfDone component={component} headingLevel={3} />

## Community implementaties

<Implementations component={component} headingLevel={3} />

<Related component={component} />

## Acceptatiecriteria

<AcIntro />

<CriteriaList headingLevel={4} testCategory="Toegankelijkheid algemeen">
  {general.map(({ component: Wcag, ...item }) => (
    <CriteriaListItem {...item}>
      <Wcag />
    </CriteriaListItem>
  ))}
</CriteriaList>

<CriteriaList headingLevel={4} testCategory="Toegankelijkheid visueel ontwerp">
  {visualDesign.map(({ component: Wcag, ...item }) => (
    <CriteriaListItem {...item}>
      <Wcag />
    </CriteriaListItem>
  ))}
</CriteriaList>

<CriteriaList headingLevel={4} testCategory="Toegankelijkheid zoom en herschalen">
  {zoom.map(({ component: Wcag, ...item }) => (
    <CriteriaListItem {...item}>
      <Wcag />
    </CriteriaListItem>
  ))}
</CriteriaList>

<CriteriaList headingLevel={4} testCategory="Toegankelijkheid toetsenbord">
  {keyboard.map(({ component: Wcag, ...item }) => (
    <CriteriaListItem {...item}>
      <Wcag />
    </CriteriaListItem>
  ))}
</CriteriaList>

<CriteriaList headingLevel={4} testCategory="Toegankelijkheid screenreader">
  {screenreader.map(({ component: Wcag, ...item }) => (
    <CriteriaListItem {...item}>
      <Wcag />
    </CriteriaListItem>
  ))}
</CriteriaList>

<HelpImproveComponent component={component} headingLevel={2} />
