{/* @license CC0-1.0 */}

import { Canvas } from "@site/src/components/Canvas/Canvas";
import { Guideline } from "@site/src/components/Guideline";

## Voorbeelden

<Guideline appearance="do" title="Het label altijd tonen en een description gebruiken als aanvullende informatie.">
  <Canvas language="html">
    {() => (
      <>
        <p>
          <label htmlFor="lievelingskleur1">Wat is je lievelingskleur?</label>
        </p>
        <p id="description-kleur1">Vul de naam van een kleur in.</p>
        <p>
          <input aria-describedby="description-kleur1" id="lievelingskleur1" name="lievelingskleur" type="text" />
        </p>
      </>
    )}
  </Canvas>
</Guideline>

<Guideline appearance="do" title="Het label altijd tonen en een placeholder als korte hint gebruiken.">
  <Canvas language="html">
    {() => (
      <>
        <p>
          <label htmlFor="lievelingskleur2">Wat is je lievelingskleur?</label>
        </p>
        <p>
          <input id="lievelingskleur2" type="text" name="lievelingskleur" placeholder="Vul een kleurnaam in" />
        </p>
      </>
    )}
  </Canvas>
</Guideline>

<Guideline appearance="dont" title="Het label onzichtbaar maken.">
  <Canvas language="html">
    {() => (
      <>
        <p>
          <label className="sr-only" htmlFor="lievelingskleur3">
            Wat is je lievelingskleur?
          </label>
        </p>
        <p>
          <input id="lievelingskleur3" name="lievelingskleur" placeholder="Wat is je lievelingskleur?" type="text" />
        </p>
      </>
    )}
  </Canvas>
</Guideline>

<Guideline
  appearance="dont"
  title="Placeholdertekst heel lang maken zodat een deel uit het formulierveld valt en daardoor onleesbaar wordt. Gebruik in dat geval een description."
>
  <Canvas language="html">
    {() => (
      <>
        <p>
          <label htmlFor="lievelingskleur4">Wat is je lievelingskleur?</label>
        </p>
        <p>
          <input
            id="lievelingskleur4"
            name="lievelingskleur"
            placeholder="Geef is je lievelingskleur? Bijvoorbeeld rood, wit, geel of blauw."
            type="text"
          />
        </p>
      </>
    )}
  </Canvas>
</Guideline>
