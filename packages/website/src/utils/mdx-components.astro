---
import { OverviewPage } from '@components/overview-page/overview-page.tsx';
import { DocCardList } from '@components/doc-card-list/doc-card-list';
import ComponentOverview from '@components/docusaurus-mocks/ComponentOverview.astro';
import ComponentAnatomy from '@components/docusaurus-mocks/ComponentAnatomy.astro';
import { CriteriaList } from '@components/criteria-list/criteria-list';
import { CriteriaListItem } from '@components/criteria-list/criteria-list-item';
import { Checklist, ChecklistItem } from '@site/src/components/Checklist';
import { RelayModel, RelaySection } from '@components/docusaurus-mocks/RelayModel.astro';
import * as Amsterdam from '@components/docusaurus-mocks/AmsterdamComponents.astro';
import * as Card from '@components/docusaurus-mocks/Card.astro';

export const MDXComponents = async ({ navigationItems } = {}) => {
  return {
    ...Amsterdam,
    Card,
    Checklist,
    ChecklistItem,
    ComponentOverview,
    ComponentAnatomy,
    CriteriaList,
    CriteriaListItem,
    OverviewPage: (props) => OverviewPage({ ...props, pages: navigationItems || [] }),
    DocCardList,
    RelayModel,
    RelaySection,
  };
};
---
